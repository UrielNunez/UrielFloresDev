.images{
    margin: auto 0;
    width: 100%;
    max-height: 250px;
    overflow-y: auto;
    display: grid; /* Usar un sistema de cuadrícula */
    grid-template-columns: repeat(4, 1fr); /* Tres columnas */
    gap: 10px; /* Espacio entre las imágenes */
    justify-content: center;
    align-items: center;
    
    
}
.images img{
    
    max-width: 85px; /* Se establece un ancho máximo para evitar que la imagen se desborde de su contenedor */
    max-height: 85px;
    object-fit: cover;
    padding: 5px; /* Agregar relleno para las imágenes más pequeñas */
    box-sizing:border-box; /* Incluir el relleno en el cálculo del tamaño total */
}